module.exports = {
  // Whether the browser should remain open after running tests:
  'persistent': false,
  // Display test results in expanded form:
  'expanded': false,

  // Output verbosity:
  'verbose': false 

};
